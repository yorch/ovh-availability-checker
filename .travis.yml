language: node_js
node_js:
  - 12

services:
  - docker

before_install:
  - docker build -t yorch/ovh-availability-checker .

script:
  - docker run -d yorch/ovh-availability-checker ovh-availability-checker
  - docker logs ovh-availability-checker
  - docker ps -a
